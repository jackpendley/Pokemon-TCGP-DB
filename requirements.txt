rapidfuzz
requests
jsonschema
# Chrome TLS impersonation — bypasses Cloudflare fingerprinting (optional but recommended)
curl-cffi
beautifulsoup4
lxml
# Browser automation for Pokemon Zone sync
playwright
# After installing: run `playwright install chromium`
# Hosted publish to Supabase (optional — only for scripts/publish_to_supabase.py)
supabase
